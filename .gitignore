# macOS system files
.DS_Store

# Jupyter
.ipynb_checkpoints/

# Python cache
__pycache__/
*.pyc

# Raw data (do not commit datasets)
data/raw/

# Models / large artifacts
models/

# Large model artifacts (do not commit)
app/pred_df.parquet
app/movie_map.parquet
*.parquet

git rm --cached app/pred_df.parquet
git rm --cached app/movie_map.parquet
git rm --cached *.parquet
git commit -m "chore: ignore large parquet artifacts"
git push

